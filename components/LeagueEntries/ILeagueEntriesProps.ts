export interface ILeagueEntriesProps {
  entryNumber: number;
  isEliminated?: boolean;
  isPickSet?: boolean;
  survivors: number;
  leagueName: string;
  totalPlayers: number;
  weekNumber: number;
}
